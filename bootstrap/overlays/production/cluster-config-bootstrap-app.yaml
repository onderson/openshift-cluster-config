apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: openshift-cluster-config-bootstrap
  namespace: argocd
  labels:
    gitops.ownedBy: openshift-cluster-config
spec:
  destination:
    namespace: argocd
    server: https://kubernetes.default.svc
  project: default
  source:
    path: clusters/production/argocd/bootstrap
    repoURL: <TO_REPLACE> #with something ending like openshift-cluster-config
    targetRevision: main
  syncPolicy:
    automated:
      prune: false
      selfHeal: true